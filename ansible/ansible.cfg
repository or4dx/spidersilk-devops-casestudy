[defaults]
inventory  = inventory/local.ini
roles_path = roles
# group_vars/ lives at inventory/group_vars/all/ (adjacent to the inventory file).
# Ansible loads all files in that directory for the 'all' group, including the
# vault-encrypted all.vault.yaml.
